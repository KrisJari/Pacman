export default class dots {
  constructor(startPosition, radius,tileSize) {
    this.x = startPosition.x;
    this.y = startPosition.y;
    this.tileSize = tileSize;
    this.radius = radius;
    this.dotsArray = [];
  }

 


}
